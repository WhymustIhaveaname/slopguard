[project]
name = "slopguard"
version = "0.1.0"
description = "检测 Claude Code 输出里的 AI 腔中文,命中后打回重说的 Claude Code 插件"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]
