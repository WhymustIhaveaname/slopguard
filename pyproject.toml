[project]
name = "slopguard"
version = "0.4.0"
description = "检测 Claude Code 输出里的 AI 腔和翻译腔,命中后打回纠正的 Claude Code 插件"
requires-python = ">=3.9"
dependencies = ["pyyaml"]

[dependency-groups]
dev = ["pytest>=8"]
